/// [CustomTimer] is an entity that represents a timer.
class CustomTimer {
  CustomTimer({
    required this.id,
    required this.name,
    required this.duration,
  });

  final String id;
  final String name;
  final Duration duration;
}
